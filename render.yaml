services:
  - type: web
    name: mediator-on-call
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn mediators_on_call.wsgi:application --bind 0.0.0.0:$PORT --config gunicorn.conf.py"
